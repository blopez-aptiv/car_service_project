{

    "version": "0.2.0",    
    "configurations": [    
    
    {    
    "name": "Python",    
    "type": "python",    
    "request": "launch",    
    "cwd": "${workspaceFolder}/src",    
    "program": "${workspaceFolder}/src/main.py",
    
    //"args": ["daqtocloud.cc.telemetry.CT9999-101"],
    
    "env": {    
    "MYSQL_USERNAME": "root", // The host that RabbitMQ is hosted on    
    "MYSQL_PASSWORD": "dataApp_ctq2022", // The port on the host that RabbitMQ is bound to    
    "MYSQL_DB": "servicio_carro", // The username for logging into RabbitMQ    
    "MYSQL_HOST": "localhost"  
    }    
    }    
    ]    
    }